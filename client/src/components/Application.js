import React, { Component, } from "react";
import { Container, Header, } from "semantic-ui-react";

class Application extends Component {
  render() {
    return (
      <Container>
        <br />
        <Header as="h1">Application Show</Header>
      </Container>
    );
  };
};

export default Application;
